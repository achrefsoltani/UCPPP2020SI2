{% extends 'base.html.twig' %}

{% block title %}Matiere{% endblock %}

{% block body %}
    <h1>Matiere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matiere.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ matiere.nom }}</td>
            </tr>
            <tr>
                <th>Coef</th>
                <td>{{ matiere.coef }}</td>
            </tr>
        </tbody>
    </table>
<div class="btn-group" role="group" aria-label="Basic example">

    <a type="button" class="btn btn-primary" href="{{ path('matiere_edit', {'id': matiere.id}) }}">Modifier</a>

    <a type="button" class="btn btn-info" href="{{ path('matiere_index') }}">retour à la liste </a>
    {{ include('matiere/_delete_form.html.twig') }}
</div>




{% endblock %}
